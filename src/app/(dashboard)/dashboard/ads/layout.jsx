/**
 * Ads section layout — padded content area inside dashboard shell.
 * @module app/(dashboard)/dashboard/ads/layout
 */
export default function DashboardAdsLayout({ children }) {
   return <div className="mx-auto max-w-6xl">{children}</div>;
}
