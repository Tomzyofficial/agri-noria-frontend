export const metadata = {
   title: "Training Hub — Vendor Materials",
   description: "Browse curated training materials from certified vendor partners",
};

export default function RootLayout({ children }) {
   return <div>{children}</div>;
}
